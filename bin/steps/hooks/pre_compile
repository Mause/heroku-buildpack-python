#!/usr/bin/env bash

if [ -f bin/pre_compile ]; then
    echo "-----> Running pre-compile hook"
    chmod +x bin/pre_compile
    sub-env bin/pre_compile
fi

chmod +x bin/steps/hooks/node-compile
sub-env bin/steps/hooks/node-compile

npm install -g ember-precompile
